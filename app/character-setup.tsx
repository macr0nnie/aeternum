// Expo Router requires a default export from every file in app/.
// The actual component lives in src/components/CharacterSetup.tsx
// and is rendered as an overlay from _layout.tsx, not as a routed screen.
export { CharacterSetup as default } from '@/components/CharacterSetup'
